{
  "id": "C-021",
  "file": "templates/login.html",
  "role": "coder",
  "title": "Login page",
  "interface": "Extends base.html. Dev mode form: POST /auth/dev-login with input name=email and hidden input next (value from request.args.get('next','/')), button 'Přihlásit'. Also a link 'Přihlásit účtem Microsoft 365' to /auth/login only when config.AUTH_MODE == 'oidc' (use the `config` Jinja global). Czech.",
  "uses": {
    "base": "templates/base.html: Jinja layout with blocks title and content; header nav (Moje zařízení '/', for admins: /admin/assets, /admin/handovers, /admin/employees, /admin/audit), current user e-mail and a logout form POST /auth/logout; flashed messages via get_flashed_messages(with_categories=true); links /static/style.css"
  },
  "database": "",
  "acceptance": "",
  "forbidden": [],
  "depends_on": [
    "C-020"
  ],
  "budget": {
    "iterations": 12
  },
  "notes": ""
}