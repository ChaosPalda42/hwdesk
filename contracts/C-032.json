{
  "id": "C-032",
  "file": "src/app.py",
  "role": "integrator",
  "title": "Application factory – register repairs blueprint",
  "interface": "def create_app(overrides: dict | None = None) -> Flask:\n    # existing blueprint registrations …\n    # NEW registration for repairs API\n    from src.api.repairs import bp as repairs_bp\n    app.register_blueprint(repairs_bp, url_prefix='/api/v1')\n    return app",
  "uses": {
    "repairs_api": "src/api/repairs.py: bp"
  },
  "database": "",
  "acceptance": "tests",
  "forbidden": [],
  "depends_on": [],
  "budget": {
    "iterations": 12
  },
  "notes": "Only the additional blueprint registration line is added; other existing logic unchanged."
}
