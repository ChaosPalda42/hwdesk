# Factory v2 project configuration. Roles map to models on the local llama-server (:8080).
[run]
test_command = ['env', 'HWDESK_ALLOW_MISSING_BLUEPRINTS=1', 'uv', 'run', 'pytest', '-q']
iterations_per_contract = 12
max_run_iterations = 800

# 2026-09-21: harness defaults = qwen3.8-flash-next on llama-server (coder/tester
# without thinking, triage low); no [roles.*] here means "use the defaults".
